---
description: Scaffold a new Next.js API route for FixNow. Runs the scaffold-api skill.
argument-hint: <method> <path>, vd: POST /api/appointments
---

# /api

Tao API route Next.js moi.

1. Goi skill **scaffold-api** voi tham so: `$ARGUMENTS`.
2. Hoi user: auth requirement, input schema, response shape.
3. Tao file route.ts + zod schema trong lib/validations/.
4. Cap nhat docs/routes.md (bang API endpoints).
5. Test bang curl example.

Khi logic phuc tap -> spawn agent **fixnow-api-builder**.
